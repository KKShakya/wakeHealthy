/// <reference types="react" />
import { Props } from '../types';
declare type SlideProps = {
    activeIndex: number;
    itemsCount: number;
    renderSlideInfo?: Props['renderSlideInfo'];
};
export declare const SlideInfo: ({ activeIndex, itemsCount, renderSlideInfo }: SlideProps) => JSX.Element;
export {};
