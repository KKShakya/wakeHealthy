import { CSSProperties, ReactNode } from 'react';
declare type Props = {
    item: ReactNode;
    className?: string;
    styles?: CSSProperties;
};
export declare const StageItem: ({ item, className, styles }: Props) => JSX.Element;
export {};
