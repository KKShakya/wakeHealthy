import { MouseEventHandler } from 'react';
import { State, Props } from '../types';
export declare const DotsNavigation: ({ state, onClick, onMouseEnter, onMouseLeave, controlsStrategy, renderDotsItem, }: DotsNavigationProps) => JSX.Element;
declare type DotsNavigationProps = {
    state: State;
    controlsStrategy?: string;
    onClick: (index: number) => void;
    onMouseEnter?: MouseEventHandler;
    onMouseLeave?: MouseEventHandler;
    renderDotsItem?: Props['renderDotsItem'];
};
export {};
